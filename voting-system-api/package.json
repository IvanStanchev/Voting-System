{
  "name": "voting-system-api",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ voting-system-api",
    "create-local": "graph create --node http://localhost:8020/ voting-system-api",
    "remove-local": "graph remove --node http://localhost:8020/ voting-system-api",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 voting-system-api",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.38.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
